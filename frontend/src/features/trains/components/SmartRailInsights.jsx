import { motion } from 'framer-motion';
const steps = ['Search', 'Compare', 'Reserve', 'Travel', 'Arrive'];
export default function SmartRailInsights() { return <section className="sr-journey-flow"><div className="sr-section-copy"><span>WHY SOUTHRAIL</span><h2>A reservation journey designed around confidence</h2></div><div className="sr-timeline-story">{steps.map((step, index) => <motion.div className="sr-story-stop" key={step} initial={{ opacity: 0, y: 28 }} whileInView={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.08 }} viewport={{ once: true }}><em>{String(index + 1).padStart(2, '0')}</em><h3>{step}</h3><p>{copyFor(step)}</p></motion.div>)}</div></section>; }
function copyFor(step) { return { Search: 'Command-center inputs reveal routes without visual clutter.', Compare: 'Fare, time, and availability are staged like premium departures.', Reserve: 'Clear seat decisions reduce hesitation at checkout.', Travel: 'Journey details stay readable from mobile to desktop.', Arrive: 'South Indian destinations become part of the brand story.' }[step]; }
